# 作りたいもの

Slackのtimesチャンネルから投稿内容を読み取り、Toggleに記録するツールを作りたい。
とりあえず最低限の機能のみを持つv0.1を作成する。

1. ローカルでCLIで実行
2. 今日1日分だけを記録する
3. SlackのチャンネルIDなどは環境変数から読み取る
4. Slackの記法は使いやすいように整えていきたいが、とりあえずv0.1では読み取りやすい記法でSlackに書き、最も書き込みやすいようにtoggleに書き込む

# cannonicalについて

/cannonicalに参考になりそうな別PJ（Slackから情報を読み取りDBに同期）を置いた。
以下の項目は/cannonicalを参考に実装を進めて欲しい

1. DDDについて学ぶ。インターフェースを使った依存性の逆転など
2. ローカルのテスト環境を整える。並列実行による高速化、テーブルドリブンテストのやり方など
3. 環境変数周りの整え方。configやtomlの仕組みなど
4. 引数爆発が起きた時のFOP（ファンクショナルオプショナルパターン）について
5. ロガーの実装
6. 並列処理、ゴルーチン
7. エラーハンドリングの設計
8. グレースフルシャットダウン

現時点ではDB不要だが、必要そうなら以下も参考にする

1. DBまわり。トランザクションによる排他制御の実装。コンテキストを使ってトランザクション用のコネクションと通常のコネクションを切り替えるテク。
2. テスト時トランザクションをロールバックしてテスト用のデータを初期化するテク
